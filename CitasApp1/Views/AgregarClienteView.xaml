<UserControl x:Class="CitasApp1.Views.AgregarClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CitasApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"></Setter>
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="24"></Setter>
            <Setter Property="HorizontalAlignment" Value="Right"></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Width="400" Background="White"  >
        <StackPanel>
            <TextBlock Margin="10" FontSize="30" Text="Agregar cliente" HorizontalAlignment="Center" FontWeight="Bold"></TextBlock>
            <WrapPanel Margin="10" Orientation="Horizontal">
                <TextBlock Text="Nombre:" Width="120"></TextBlock>
                <TextBox Text="Ramiro Ronaldo Rodriguez Valverde" Width="260" HorizontalAlignment="Stretch" TextWrapping="Wrap"></TextBox>
            </WrapPanel>
            <WrapPanel Margin="10">
                <TextBlock Text="Telefono:" Width="120"></TextBlock>
                <TextBox Width="260"></TextBox>
            </WrapPanel>
            <WrapPanel Margin="10">
                <TextBlock Margin="0,0,10,0">
                    <Run Text="Correo"></Run>
                    <LineBreak/>
                    <Run Text="electrónico:"></Run>
                </TextBlock>
                <TextBox HorizontalAlignment="Left" Width="245"></TextBox>
            </WrapPanel>
            <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,10,10,0">
                <Button FontSize="24" Width="110" Margin="10" FontWeight="DemiBold">Agregar</Button>
                <Button FontSize="24" Width="110" Margin="10" FontWeight="DemiBold">Cancelar</Button>
            </WrapPanel>
            <TextBlock FontSize="18" TextWrapping="Wrap" MaxWidth="400" HorizontalAlignment="Center"></TextBlock>
        </StackPanel>
        <!--<Border VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Background="#95D5B2" Panel.ZIndex="-1" Height="30" ></Border>-->
    </Grid>

</UserControl>
